#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int n;
	cin>>n;
	int val;
	/* O(N) time, O(N) space - hashing method using map
	unordered_map<int,int> um;
	for(int i=0;i<n+1;i++){
		cin>>val;
		um[val]++;
	}
	for(auto it = um.begin();it!=um.end();it++){
		if(it->second>1){
			cout<<it->first<<"\n";
		}
	}*/

	/* LINKED LIST METHOD - O(N) time complexity and O(1) time complexity */

}